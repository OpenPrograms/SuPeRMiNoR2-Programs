m = {}
-------------
m.loop_speed = 1.5
--How long to wait before the next loop, in seconds. (Update time)

m.estimate_ticks = true
--Estimate rate in ticks per second.

m.ticks_scaler = 20
--Increase if the rate reads too high, decrease if the rate reads to0 slow, default = 20

m.ticks_use_world_time = false --currently not functional
--enabling this ignores the tick scaler above

m.scale = 1.0
--Screen Scaling, 1 does nothing, 2 doubles the size, etc

m.display_precision = 1
--How many units to show on the energy readouts

m.display_units = false
--Show the amount of energy stored in plain form (Not just percentages)

m.startup_delay = 1
--How long to wait after startup before going in to the program.

-----------------------------------------------Glasses Area

m.glasses_xoffset = 2
--Change this higher to move it more to the left

m.glasses_yoffset = 2
--Change this higher to move it down

-------------System Tweaks, leave these alone if you do not know what they do

m.nokeepalive = false

--------------------------------------------------------------------------------------------
m.config_version = "0.9" --Leave this alone
return m